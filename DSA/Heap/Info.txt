A heap is a special tree-based data structure that follows a specific ordering property. It is mainly used to quickly access the maximum or minimum element.

Heaps are of two types: max heap and min heap. In a max heap, the parent node is always greater than its children, while in a min heap, the parent node is always smaller.

Heaps are usually implemented using arrays and are widely used where priority-based processing is required.